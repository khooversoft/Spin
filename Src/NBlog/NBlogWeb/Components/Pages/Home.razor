@page "/"
@rendermode InteractiveServer

<PageTitle>NBlog</PageTitle>

@code {
    [Inject]
    public NavigationManager _navManager { get; init; } = null!;

    protected override void OnAfterRender(bool firstRender)
    {
        _navManager.NavigateTo("/summary/home", true);
    }
}